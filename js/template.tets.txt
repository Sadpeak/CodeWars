const func = require('./')


test('nameTest 1', () => {
    expect(func()).toEqual()
})

test('nameTest 2', () => {
    expect(func()).toEqual()
})

test('nameTest 3', () => {
    expect(func()).toEqual()
})